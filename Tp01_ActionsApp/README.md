# React Native

## Installation
`npm install`

## Run on emulator
- `npm start`
- localhost:port
- select `local`
- enable debug mode and file transfer on phone
- select `run on android device/emulator`